### GetStudentByID2

```sql
select * from student where id = {{.}}
```

### GetStudentByID

>通过ID查找学生

```sql
select * from student where id = {{.id}}
```

### GetStudentByID3

>通过ID查找学生

```sql
select * from student where id = {{.id}}
```
